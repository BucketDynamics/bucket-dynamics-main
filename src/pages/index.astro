---
// Bucket Dynamics — Modular Drone Systems
// Astro component — drop into src/pages/index.astro
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Bucket Dynamics — Modular Drone Systems</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;400;500&family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    :root {
      --black: #060912;
      --white: #eef1f8;
      --gray: #6b7a99;
      --light: #dce3f0;
      --accent: #3d8eff;
      --accent-glow: rgba(61, 142, 255, 0.18);
      --accent-dim: rgba(61, 142, 255, 0.4);
      --mid: #0d1120;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }

    body {
      background: var(--black);
      color: var(--white);
      font-family: 'DM Sans', sans-serif;
      font-weight: 300;
      overflow-x: hidden;
      cursor: none;
    }

    /* ── CURSOR ── */
    .cursor {
      position: fixed;
      width: 10px; height: 10px;
      background: var(--accent);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      transition: transform 0.15s ease;
      transform: translate(-50%, -50%);
    }
    .cursor-ring {
      position: fixed;
      width: 36px; height: 36px;
      border: 1px solid var(--accent-dim);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9998;
      transition: transform 0.4s ease, width 0.3s, height 0.3s, border-color 0.3s;
      transform: translate(-50%, -50%);
    }

    /* ── NAV ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 1.5rem 4rem;
      border-bottom: 1px solid rgba(61,142,255,0.1);
      backdrop-filter: blur(20px);
      background: rgba(6,9,18,0.75);
    }
    .logo {
      font-family: 'Syne', sans-serif;
      font-weight: 800;
      font-size: 1.1rem;
      letter-spacing: 0.05em;
      text-decoration: none;
      color: var(--white);
    }
    .logo span { color: var(--accent); }
    .nav-links {
      display: flex; gap: 2.5rem;
      list-style: none;
    }
    .nav-links a {
      font-family: 'DM Mono', monospace;
      font-size: 0.72rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      text-decoration: none;
      color: var(--gray);
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--white); }
    .nav-cta {
      font-family: 'DM Mono', monospace;
      font-size: 0.72rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 0.6rem 1.4rem;
      border: 1px solid var(--accent);
      color: var(--accent);
      text-decoration: none;
      transition: background 0.2s, color 0.2s;
    }
    .nav-cta:hover { background: var(--accent); color: var(--black); }

    /* ── HERO ── */
    #home {
      min-height: 100vh;
      display: flex; flex-direction: column; justify-content: flex-end;
      padding: 0 4rem 5rem;
      position: relative;
      overflow: hidden;
    }
    .hero-bg {
      position: absolute; inset: 0;
      background:
        radial-gradient(ellipse 60% 50% at 70% 40%, rgba(61,142,255,0.07) 0%, transparent 70%),
        radial-gradient(ellipse 40% 60% at 20% 80%, rgba(61,142,255,0.04) 0%, transparent 60%);
    }
    .hero-grid {
      position: absolute; inset: 0;
      background-image:
        linear-gradient(rgba(61,142,255,0.05) 1px, transparent 1px),
        linear-gradient(90deg, rgba(61,142,255,0.05) 1px, transparent 1px);
      background-size: 80px 80px;
      mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 40%, transparent 100%);
    }
    .hero-tag {
      font-family: 'DM Mono', monospace;
      font-size: 0.7rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 2rem;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.2s forwards;
    }
    .hero-title {
      font-family: 'Syne', sans-serif;
      font-size: clamp(3.5rem, 9vw, 8rem);
      font-weight: 800;
      line-height: 0.92;
      letter-spacing: -0.03em;
      margin-bottom: 2.5rem;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.4s forwards;
    }
    .hero-title em {
      font-style: normal;
      color: transparent;
      -webkit-text-stroke: 1px rgba(61,142,255,0.35);
    }
    .hero-bottom {
      display: flex; align-items: flex-end; justify-content: space-between;
      gap: 2rem;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.6s forwards;
    }
    .hero-desc {
      max-width: 380px;
      font-size: 1rem;
      line-height: 1.7;
      color: var(--gray);
    }
    .hero-actions { display: flex; gap: 1rem; align-items: center; }
    .btn-primary {
      font-family: 'DM Mono', monospace;
      font-size: 0.75rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 1rem 2rem;
      background: var(--accent);
      color: var(--black);
      text-decoration: none;
      font-weight: 500;
      transition: opacity 0.2s, transform 0.2s;
    }
    .btn-primary:hover { opacity: 0.85; transform: translateY(-1px); }
    .btn-ghost {
      font-family: 'DM Mono', monospace;
      font-size: 0.75rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 1rem 2rem;
      border: 1px solid rgba(61,142,255,0.2);
      color: var(--gray);
      text-decoration: none;
      transition: border-color 0.2s, color 0.2s;
    }
    .btn-ghost:hover { border-color: var(--accent); color: var(--white); }

    /* ── STATS BAR ── */
    .stats-bar {
      border-top: 1px solid rgba(61,142,255,0.1);
      border-bottom: 1px solid rgba(61,142,255,0.1);
      display: grid; grid-template-columns: repeat(4, 1fr);
      opacity: 0;
      animation: fadeUp 0.8s ease 0.8s forwards;
    }
    .stat {
      padding: 2rem 4rem;
      border-right: 1px solid rgba(61,142,255,0.1);
    }
    .stat:last-child { border-right: none; }
    .stat-num {
      font-family: 'Syne', sans-serif;
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--white);
    }
    .stat-num span { color: var(--accent); }
    .stat-label {
      font-family: 'DM Mono', monospace;
      font-size: 0.65rem;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--gray);
      margin-top: 0.3rem;
    }

    /* ── SECTIONS ── */
    section { padding: 7rem 4rem; }

    .section-tag {
      font-family: 'DM Mono', monospace;
      font-size: 0.65rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 1rem;
    }
    .section-title {
      font-family: 'Syne', sans-serif;
      font-size: clamp(2rem, 4vw, 3.5rem);
      font-weight: 700;
      line-height: 1.05;
      letter-spacing: -0.02em;
    }

    /* ── ABOUT ── */
    #about {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 6rem;
      align-items: center;
    }
    .about-visual {
      position: relative;
      aspect-ratio: 1;
      border: 1px solid rgba(61,142,255,0.12);
      display: flex; align-items: center; justify-content: center;
      overflow: hidden;
    }
    .about-visual::before {
      content: '';
      position: absolute; inset: 0;
      background: radial-gradient(circle at 50% 50%, rgba(61,142,255,0.08), transparent 70%);
    }
    .drone-diagram { width: 70%; height: 70%; position: relative; }
    .drone-diagram svg { width: 100%; height: 100%; }
    .about-content { padding: 2rem 0; }
    .about-text {
      font-size: 1.05rem;
      line-height: 1.8;
      color: var(--gray);
      margin: 2rem 0;
    }
    .about-pillars {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 1px;
      background: rgba(61,142,255,0.1);
      margin-top: 2.5rem;
    }
    .pillar { background: var(--black); padding: 1.5rem; }
    .pillar-icon {
      font-family: 'DM Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.15em;
      color: var(--accent);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }
    .pillar h4 {
      font-family: 'Syne', sans-serif;
      font-size: 0.95rem;
      font-weight: 600;
      margin-bottom: 0.4rem;
    }
    .pillar p { font-size: 0.82rem; color: var(--gray); line-height: 1.5; }

    /* ── SERVICES ── */
    #services { background: var(--mid); }
    .services-header {
      display: flex; align-items: flex-end; justify-content: space-between;
      margin-bottom: 4rem; gap: 2rem;
    }
    .services-sub { max-width: 300px; font-size: 0.9rem; color: var(--gray); line-height: 1.6; }
    .services-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1px;
      background: rgba(61,142,255,0.08);
    }
    .service-card {
      background: var(--mid);
      padding: 3rem 2.5rem;
      position: relative;
      overflow: hidden;
      transition: background 0.3s;
    }
    .service-card::after {
      content: '';
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 2px;
      background: var(--accent);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.4s ease;
    }
    .service-card:hover { background: #111827; }
    .service-card:hover::after { transform: scaleX(1); }
    .service-num {
      font-family: 'DM Mono', monospace;
      font-size: 0.65rem;
      color: var(--accent);
      letter-spacing: 0.15em;
      margin-bottom: 2rem;
    }
    .service-card h3 {
      font-family: 'Syne', sans-serif;
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 1rem;
      line-height: 1.2;
    }
    .service-card p { font-size: 0.875rem; color: var(--gray); line-height: 1.7; }
    .service-tags { margin-top: 2rem; display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .tag {
      font-family: 'DM Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 0.3rem 0.7rem;
      border: 1px solid rgba(61,142,255,0.2);
      color: var(--gray);
    }

    /* ── CONTACT ── */
    #contact {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 6rem; align-items: start;
    }
    .contact-info { padding-top: 1rem; }
    .contact-intro { font-size: 1rem; color: var(--gray); line-height: 1.7; margin: 1.5rem 0 3rem; }
    .contact-items { display: flex; flex-direction: column; gap: 1.5rem; }
    .contact-item {
      display: flex; gap: 1.5rem; align-items: flex-start;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid rgba(61,142,255,0.08);
    }
    .contact-item:last-child { border-bottom: none; }
    .contact-item-label {
      font-family: 'DM Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--accent);
      min-width: 80px;
      padding-top: 0.1rem;
    }
    .contact-item-val { font-size: 0.9rem; color: var(--gray); line-height: 1.6; }
    .contact-form { display: flex; flex-direction: column; gap: 1.2rem; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
    .form-group { display: flex; flex-direction: column; gap: 0.5rem; }
    .form-group label {
      font-family: 'DM Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--gray);
    }
    .form-group input,
    .form-group textarea,
    .form-group select {
      background: rgba(61,142,255,0.05);
      border: 1px solid rgba(61,142,255,0.15);
      color: var(--white);
      padding: 0.9rem 1rem;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.875rem;
      outline: none;
      transition: border-color 0.2s;
      resize: none;
      appearance: none;
    }
    .form-group input:focus,
    .form-group textarea:focus,
    .form-group select:focus { border-color: var(--accent); }
    .form-group textarea { min-height: 130px; }
    .form-group select option { background: #0d1120; }
    .btn-submit {
      font-family: 'DM Mono', monospace;
      font-size: 0.75rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      padding: 1.1rem 2.5rem;
      background: var(--accent);
      color: var(--black);
      border: none;
      font-weight: 500;
      cursor: none;
      align-self: flex-start;
      transition: opacity 0.2s, transform 0.2s;
    }
    .btn-submit:hover { opacity: 0.85; transform: translateY(-1px); }

    /* ── FOOTER ── */
    footer {
      border-top: 1px solid rgba(61,142,255,0.1);
      padding: 2.5rem 4rem;
      display: flex; align-items: center; justify-content: space-between;
    }
    .footer-logo { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 0.95rem; color: var(--white); }
    .footer-logo span { color: var(--accent); }
    .footer-copy {
      font-family: 'DM Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.1em;
      color: var(--gray);
      text-transform: uppercase;
    }
    .footer-links { display: flex; gap: 2rem; }
    .footer-links a {
      font-family: 'DM Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--gray);
      text-decoration: none;
      transition: color 0.2s;
    }
    .footer-links a:hover { color: var(--white); }

    /* ── DIVIDER ── */
    .divider { width: 40px; height: 1px; background: var(--accent); margin: 1.5rem 0; }

    /* ── ANIMATIONS ── */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes spin {
      from { transform: rotate(0deg); }
      to   { transform: rotate(360deg); }
    }
    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }
    .fade-in.visible { opacity: 1; transform: translateY(0); }

    /* ── RESPONSIVE ── */
    @media (max-width: 900px) {
      nav { padding: 1.2rem 2rem; }
      .nav-links { display: none; }
      .mobile-menu-btn {
        display: flex;
        flex-direction: column;
        gap: 5px;
        background: none;
        border: none;
        cursor: pointer;
        padding: 4px;
      }
      .mobile-menu-btn span {
        display: block;
        width: 22px; height: 1px;
        background: var(--white);
        transition: transform 0.3s, opacity 0.3s;
      }
      .mobile-menu-btn.open span:nth-child(1) { transform: translateY(6px) rotate(45deg); }
      .mobile-menu-btn.open span:nth-child(2) { opacity: 0; }
      .mobile-menu-btn.open span:nth-child(3) { transform: translateY(-6px) rotate(-45deg); }
      .mobile-nav {
        display: none;
        position: fixed;
        top: 0; left: 0; right: 0; bottom: 0;
        background: var(--black);
        z-index: 99;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 2.5rem;
      }
      .mobile-nav.open { display: flex; }
      .mobile-nav a {
        font-family: 'Syne', sans-serif;
        font-size: 2rem;
        font-weight: 700;
        color: var(--white);
        text-decoration: none;
        letter-spacing: -0.02em;
      }
      .mobile-nav a:hover { color: var(--accent); }
      #home, section { padding: 5rem 2rem; }
      #home { padding-bottom: 3rem; }
      #about, #contact { grid-template-columns: 1fr; gap: 3rem; }
      .about-visual { display: none; }
      .services-grid { grid-template-columns: 1fr; }
      .stats-bar { grid-template-columns: 1fr 1fr; }
      .services-header { flex-direction: column; }
      .form-row { grid-template-columns: 1fr; }
      footer { flex-direction: column; gap: 1.5rem; text-align: center; }
      .hero-bottom { flex-direction: column; align-items: flex-start; }
      .stat { padding: 1.5rem 2rem; }
    }
    @media (min-width: 901px) {
      .mobile-menu-btn { display: none; }
    }
  </style>
</head>
<body>

  <div class="cursor" id="cursor"></div>
  <div class="cursor-ring" id="cursorRing"></div>

  <!-- Mobile Nav Overlay -->
  <nav id="mobile-nav-overlay" class="mobile-nav" aria-hidden="true">
    <a href="#home"     onclick="closeMobileNav()">Home</a>
    <a href="#about"    onclick="closeMobileNav()">About</a>
    <a href="#services" onclick="closeMobileNav()">Products</a>
    <a href="#contact"  onclick="closeMobileNav()">Contact</a>
  </nav>

  <!-- NAV -->
  <nav>
    <a href="#home" class="logo">Bucket<span>.</span>Dynamics</a>
    <ul class="nav-links">
      <li><a href="#home">Home</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#services">Products</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
    <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="Toggle menu">
      <span></span><span></span><span></span>
    </button>
    <a href="#contact" class="nav-cta">Get in Touch</a>
  </nav>

  <!-- HERO -->
  <section id="home">
    <div class="hero-bg"></div>
    <div class="hero-grid"></div>
    <p class="hero-tag">— Next-Gen Aerial Systems</p>
    <h1 class="hero-title">
      Modular.<br>
      <em>Infinite.</em><br>
      Flight.
    </h1>
    <div class="hero-bottom">
      <p class="hero-desc">Bucket Dynamics engineers modular drone platforms that adapt to any mission — reconfigurable, field-swappable, and built for the future of autonomous flight.</p>
      <div class="hero-actions">
        <a href="#services" class="btn-primary">Explore Platform</a>
        <a href="#about" class="btn-ghost">Our Story</a>
      </div>
    </div>
  </section>

  <!-- STATS BAR -->
  <div class="stats-bar">
    <div class="stat">
      <div class="stat-num">6<span>+</span></div>
      <div class="stat-label">Swappable Modules</div>
    </div>
    <div class="stat">
      <div class="stat-num">45<span>min</span></div>
      <div class="stat-label">Flight Time</div>
    </div>
    <div class="stat">
      <div class="stat-num">3<span>kg</span></div>
      <div class="stat-label">Payload Capacity</div>
    </div>
    <div class="stat">
      <div class="stat-num">IP<span>55</span></div>
      <div class="stat-label">Weather Rating</div>
    </div>
  </div>

  <!-- ABOUT -->
  <section id="about">
    <div class="about-visual fade-in">
      <div class="drone-diagram">
        <svg viewBox="0 0 300 300" fill="none" xmlns="http://www.w3.org/2000/svg">
          <line x1="150" y1="150" x2="60"  y2="60"  stroke="rgba(61,142,255,0.15)" stroke-width="2"/>
          <line x1="150" y1="150" x2="240" y2="60"  stroke="rgba(61,142,255,0.15)" stroke-width="2"/>
          <line x1="150" y1="150" x2="60"  y2="240" stroke="rgba(61,142,255,0.15)" stroke-width="2"/>
          <line x1="150" y1="150" x2="240" y2="240" stroke="rgba(61,142,255,0.15)" stroke-width="2"/>
          <circle cx="60"  cy="60"  r="30" stroke="rgba(61,142,255,0.35)" stroke-width="1" stroke-dasharray="4 4"/>
          <circle cx="240" cy="60"  r="30" stroke="rgba(61,142,255,0.35)" stroke-width="1" stroke-dasharray="4 4"/>
          <circle cx="60"  cy="240" r="30" stroke="rgba(61,142,255,0.35)" stroke-width="1" stroke-dasharray="4 4"/>
          <circle cx="240" cy="240" r="30" stroke="rgba(61,142,255,0.35)" stroke-width="1" stroke-dasharray="4 4"/>
          <circle cx="60"  cy="60"  r="6" fill="rgba(61,142,255,0.7)"/>
          <circle cx="240" cy="60"  r="6" fill="rgba(61,142,255,0.7)"/>
          <circle cx="60"  cy="240" r="6" fill="rgba(61,142,255,0.7)"/>
          <circle cx="240" cy="240" r="6" fill="rgba(61,142,255,0.7)"/>
          <rect x="120" y="120" width="60" height="60" rx="4"
            stroke="rgba(255,255,255,0.2)" stroke-width="1.5" fill="rgba(61,142,255,0.05)"/>
          <rect x="135" y="135" width="30" height="30" rx="2"
            fill="rgba(61,142,255,0.12)" stroke="rgba(61,142,255,0.5)" stroke-width="1"/>
          <circle cx="150" cy="150" r="8" fill="rgba(61,142,255,0.9)"/>
          <line x1="150" y1="150" x2="150" y2="60"  stroke="rgba(61,142,255,0.1)" stroke-width="1" stroke-dasharray="2 6"/>
          <line x1="150" y1="150" x2="240" y2="150" stroke="rgba(61,142,255,0.1)" stroke-width="1" stroke-dasharray="2 6"/>
          <text x="42" y="56" fill="rgba(61,142,255,0.6)" font-family="monospace" font-size="7">M1</text>
          <text x="222" y="56" fill="rgba(61,142,255,0.6)" font-family="monospace" font-size="7">M2</text>
          <text x="42" y="249" fill="rgba(61,142,255,0.6)" font-family="monospace" font-size="7">M3</text>
          <text x="222" y="249" fill="rgba(61,142,255,0.6)" font-family="monospace" font-size="7">M4</text>
          <text x="132" y="148" fill="rgba(61,142,255,0.95)" font-family="monospace" font-size="6">CORE</text>
          <circle cx="150" cy="150" r="120" stroke="rgba(61,142,255,0.05)" stroke-width="1"/>
          <circle cx="150" cy="150" r="90"  stroke="rgba(61,142,255,0.04)" stroke-width="1" stroke-dasharray="3 9"/>
        </svg>
      </div>
    </div>

    <div class="about-content fade-in">
      <p class="section-tag">— About Us</p>
      <h2 class="section-title">Engineering<br>Adaptive Flight</h2>
      <div class="divider"></div>
      <p class="about-text">Bucket Dynamics was founded on a simple belief: drones shouldn't be single-purpose tools. We're building a platform where every component snaps in and out — payload, sensors, power — so operators can reconfigure in the field, not the factory.</p>
      <p class="about-text" style="margin-top: -0.5rem;">Our modular architecture means your drone evolves as your mission does.</p>

      <div class="about-pillars">
        <div class="pillar">
          <div class="pillar-icon">01 —</div>
          <h4>Field-Ready</h4>
          <p>Swap modules in under 90 seconds without tools or downtime.</p>
        </div>
        <div class="pillar">
          <div class="pillar-icon">02 —</div>
          <h4>Open Platform</h4>
          <p>Third-party module SDK lets partners build on our ecosystem.</p>
        </div>
        <div class="pillar">
          <div class="pillar-icon">03 —</div>
          <h4>Autonomous-Ready</h4>
          <p>Edge compute core built for onboard AI inference.</p>
        </div>
        <div class="pillar">
          <div class="pillar-icon">04 —</div>
          <h4>Built to Last</h4>
          <p>IP55-rated chassis with military-grade composite frame.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- SERVICES -->
  <section id="services">
    <div class="services-header fade-in">
      <div>
        <p class="section-tag">— Products & Modules</p>
        <h2 class="section-title">One Platform,<br>Every Mission</h2>
      </div>
      <p class="services-sub">The Bucket Dynamics stack is designed so each module clicks into a universal core — no cables, no custom firmware.</p>
    </div>

    <div class="services-grid">
      <div class="service-card fade-in">
        <div class="service-num">01</div>
        <h3>BD-Core<br>Flight Platform</h3>
        <p>The central chassis and compute brain. Universal dock accepts any BD-certified module. Onboard SLAM, GPS-denied navigation, and 5G telemetry built in.</p>
        <div class="service-tags">
          <span class="tag">Compute</span>
          <span class="tag">Navigation</span>
          <span class="tag">Telemetry</span>
        </div>
      </div>
      <div class="service-card fade-in">
        <div class="service-num">02</div>
        <h3>Vision<br>Module Suite</h3>
        <p>Swappable sensor heads — RGB 4K, thermal FLIR, multispectral, or LiDAR. Gimbal-stabilized and hot-swap compatible. No recalibration needed.</p>
        <div class="service-tags">
          <span class="tag">4K RGB</span>
          <span class="tag">Thermal</span>
          <span class="tag">LiDAR</span>
        </div>
      </div>
      <div class="service-card fade-in">
        <div class="service-num">03</div>
        <h3>Payload<br>Delivery System</h3>
        <p>Precision drop mechanism for cargo, samples, or equipment. Load-sensing release with configurable actuation. 3kg capacity with dynamic CG compensation.</p>
        <div class="service-tags">
          <span class="tag">3kg Capacity</span>
          <span class="tag">Precision Drop</span>
        </div>
      </div>
      <div class="service-card fade-in">
        <div class="service-num">04</div>
        <h3>Extended Power<br>Module</h3>
        <p>Clip-on auxiliary battery pack doubles flight endurance. Hot-swap capable mid-mission with zero power interruption. Supports fuel cell variant.</p>
        <div class="service-tags">
          <span class="tag">Hot-Swap</span>
          <span class="tag">45 min+</span>
          <span class="tag">Fuel Cell</span>
        </div>
      </div>
      <div class="service-card fade-in">
        <div class="service-num">05</div>
        <h3>Autonomy<br>Stack</h3>
        <p>Mission planning software with waypoint editor, geofencing, and collision avoidance. Runs onboard or synced to cloud. BVLOS-ready architecture.</p>
        <div class="service-tags">
          <span class="tag">BVLOS</span>
          <span class="tag">Geofence</span>
          <span class="tag">Cloud Sync</span>
        </div>
      </div>
      <div class="service-card fade-in">
        <div class="service-num">06</div>
        <h3>Enterprise<br>Integration</h3>
        <p>Fleet management dashboard, API access, and custom module development program. White-label options and volume pricing for enterprise deployments.</p>
        <div class="service-tags">
          <span class="tag">Fleet Mgmt</span>
          <span class="tag">API</span>
          <span class="tag">White-label</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact">
    <div class="contact-info fade-in">
      <p class="section-tag">— Contact</p>
      <h2 class="section-title">Let's Build<br>Together</h2>
      <div class="divider"></div>
      <p class="contact-intro">Whether you're a mission operator, systems integrator, or enterprise customer — we want to hear about your use case.</p>
      <div class="contact-items">
        <div class="contact-item">
          <span class="contact-item-label">Email</span>
          <span class="contact-item-val">hello@bucketdynamics.com</span>
        </div>
        <div class="contact-item">
          <span class="contact-item-label">Inquiries</span>
          <span class="contact-item-val">Enterprise, R&amp;D partnerships,<br>press &amp; media</span>
        </div>
        <div class="contact-item">
          <span class="contact-item-label">Status</span>
          <span class="contact-item-val" style="color: var(--accent);">● Early Access Open</span>
        </div>
      </div>
    </div>

    <div class="fade-in">
      <div class="contact-form">
        <div class="form-row">
          <div class="form-group">
            <label>First Name</label>
            <input type="text" placeholder="Ada" />
          </div>
          <div class="form-group">
            <label>Last Name</label>
            <input type="text" placeholder="Lovelace" />
          </div>
        </div>
        <div class="form-group">
          <label>Email</label>
          <input type="email" placeholder="ada@company.com" />
        </div>
        <div class="form-group">
          <label>Inquiry Type</label>
          <select>
            <option value="" disabled selected>Select...</option>
            <option>Product Demo</option>
            <option>Enterprise Pricing</option>
            <option>R&amp;D Partnership</option>
            <option>Press / Media</option>
            <option>Other</option>
          </select>
        </div>
        <div class="form-group">
          <label>Message</label>
          <textarea placeholder="Tell us about your mission or use case..."></textarea>
        </div>
        <button class="btn-submit" id="submitBtn">Send Message →</button>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-logo">Bucket<span>.</span>Dynamics</div>
    <div class="footer-links">
      <a href="#home">Home</a>
      <a href="#about">About</a>
      <a href="#services">Products</a>
      <a href="#contact">Contact</a>
    </div>
    <div class="footer-copy">© 2026 Bucket Dynamics. All rights reserved.</div>
  </footer>

  <script>
    // ── Custom cursor ──
    const cursor = document.getElementById('cursor');
    const ring   = document.getElementById('cursorRing');
    let mx = 0, my = 0, rx = 0, ry = 0;
    document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
    (function animateCursor() {
      cursor.style.left = mx + 'px'; cursor.style.top = my + 'px';
      rx += (mx - rx) * 0.12; ry += (my - ry) * 0.12;
      ring.style.left = rx + 'px'; ring.style.top = ry + 'px';
      requestAnimationFrame(animateCursor);
    })();
    document.querySelectorAll('a, button, input, textarea, select').forEach(el => {
      el.addEventListener('mouseenter', () => {
        ring.style.width = '60px'; ring.style.height = '60px';
        ring.style.borderColor = 'rgba(61,142,255,0.8)';
      });
      el.addEventListener('mouseleave', () => {
        ring.style.width = '36px'; ring.style.height = '36px';
        ring.style.borderColor = 'rgba(61,142,255,0.4)';
      });
    });

    // ── Scroll fade-in ──
    const observer = new IntersectionObserver(entries => {
      entries.forEach((e, i) => {
        if (e.isIntersecting) setTimeout(() => e.target.classList.add('visible'), i * 80);
      });
    }, { threshold: 0.1 });
    document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));

    // ── Rotor spin on hover ──
    document.querySelectorAll('.about-visual').forEach(el => {
      el.addEventListener('mouseenter', () => {
        el.querySelectorAll('circle[stroke-dasharray]').forEach(c => {
          c.style.animation = 'spin 2s linear infinite';
          c.style.transformOrigin = `${c.getAttribute('cx')}px ${c.getAttribute('cy')}px`;
        });
      });
      el.addEventListener('mouseleave', () => {
        el.querySelectorAll('circle[stroke-dasharray]').forEach(c => c.style.animation = '');
      });
    });

    // ── Form submit ──
    document.getElementById('submitBtn').addEventListener('click', function () {
      const inputs = document.querySelectorAll('.contact-form input, .contact-form textarea, .contact-form select');
      let valid = true;
      inputs.forEach(input => { if (!input.value.trim()) valid = false; });
      if (!valid) {
        this.textContent = 'Please fill all fields';
        this.style.background = 'rgba(61,142,255,0.5)';
      } else {
        this.textContent = 'Sent ✓';
        this.style.background = 'rgba(61,142,255,0.7)';
      }
      setTimeout(() => {
        this.textContent = 'Send Message →';
        this.style.background = '';
      }, 3000);
    });

    // ── Mobile menu ──
    const menuBtn = document.getElementById('mobileMenuBtn');
    const mobileNav = document.getElementById('mobile-nav-overlay');
    menuBtn.addEventListener('click', () => {
      const isOpen = menuBtn.classList.toggle('open');
      mobileNav.classList.toggle('open', isOpen);
      mobileNav.setAttribute('aria-hidden', String(!isOpen));
    });
    window.closeMobileNav = () => {
      menuBtn.classList.remove('open');
      mobileNav.classList.remove('open');
      mobileNav.setAttribute('aria-hidden', 'true');
    };
  </script>

</body>
</html>
